/**
 * 📐 تنظیمات ابعاد و مختصات تخته و تاس
 * 
 * همه سایزها و موقعیت‌های مربوط به تخته بازی و تاس در این فایل هستند.
 * فقط این فایل رو تغییر بده!
 */

// ----------------------------------------------------------------------
// 🎲 تخته بازی (Backgammon Board)
// ----------------------------------------------------------------------

export const BOARD_CONFIG = {
  // 📏 نسبت ابعاد
  ratio: 1.26, // نسبت عرض به ارتفاع تخته (ثابت - تغییر نده!)

  // 📐 Scale Factor - تخته را با نسبت ثابت کوچک/بزرگ می‌کند
  scale: {
    desktop: 1.0,        // 100% (سایز پایه) - دسکتاپ
    tablet: 0.9,         // 90% - تبلت
    mobile: 0.77,        // 77% - موبایل (iPhone Pro Max: 430px)
    smallMobile: 0.65,   // 65% - موبایل خیلی کوچک (iPhone SE: 375px)
  },

  // 📏 سایز پایه (base size)
  baseHeight: 600, // ارتفاع پایه در دسکتاپ (px)

  // 🔲 فاصله داخلی (Padding)
  padding: {
    desktop: 16, // فاصله از کناره‌ها در دسکتاپ (px)
    mobile: 12, // فاصله در موبایل (px)
    smallMobile: 8, // فاصله در موبایل کوچک (px)
  },

  // 🔺 مثلث‌ها (Triangles)
  triangleHeightRatio: 0.25, // نسبت ارتفاع مثلث به ارتفاع جایگاه (25% = فضای خالی)
};

// ----------------------------------------------------------------------
// ⚙️ تنظیمات دقیق المان‌های تخته
// ----------------------------------------------------------------------

export const BOARD_ELEMENTS_CONFIG = {
  // 📍 عرض جایگاه‌ها (Point Width Scale)
  pointWidth: {
    desktop: 0.9, // 90% عرض محاسبه شده
    mobile: 0.82, // 82% در موبایل
  },

  // 🔴⚪ اندازه مهره‌ها (Checker Size)
  checkerSize: {
    desktop: 0.9, // 90% عرض جایگاه
    mobile: 0.9, // 90% عرض جایگاه در موبایل
  },

  // 📊 فاصله بین مهره‌های روی هم (Stack Spacing)
  stackSpacing: {
    desktop: 0.9, // 90% عرض جایگاه
    mobile: 0.9, // 90% عرض جایگاه در موبایل
  },

  // 🔴⚪ مهره‌های bar (وسط تخته)
  barChecker: {
    desktop: 0.7, // 70% عرض جایگاه
    mobile: 0.6, // 60% عرض جایگاه در موبایل
  },

  // 📏 عرض bar (میله وسطی)
  barWidth: 0.9, // 90% عرض جایگاه
};

// ----------------------------------------------------------------------
// 🎯 نشونه‌های حرکت و تاس‌های انتخابی
// ----------------------------------------------------------------------

export const MOVE_INDICATORS_CONFIG = {
  // 🎯 نشونه دایره سبز روی جایگاه‌های مقصد
  validDestination: {
    size: 0.1, // 40% عرض جایگاه
    borderWidth: '3px', // ضخامت خط دور
    borderColor: 'success.main', // رنگ (success = سبز، primary = آبی، error = قرمز)
    backgroundOpacity: 0.1, // شفافیت پس‌زمینه (0.1 = خیلی شفاف، 0.3 = کمی پررنگ)
    verticalOffset: 1.2, // جابجایی به سمت راس مثلث (0 = وسط، 1 = نزدیک راس، 2 = خیلی نزدیک راس)
  },

  // 🎲 تاس‌های کوچک کنار مهره انتخابی
  diceIndicators: {
    size: 0.85, // 85% سایز مهره (0.7 = کوچک، 0.9 = بزرگ، 1.0 = هم‌اندازه مهره)
    distanceTop: 1.0, // فاصله از مهره‌های بالای تخته (تاس‌ها پایین میفتن) - (1 = نزدیک، 2.5 = متوسط، 4 = دور)
    distanceBottom: 2.6, // فاصله از مهره‌های پایین تخته (تاس‌ها بالا میفتن) - (1 = نزدیک، 2.5 = متوسط، 4 = دور)
    horizontalSpacing: 0.9, // فاصله افقی تاس‌ها از مرکز (0.7 = نزدیک هم، 1.2 = دور از هم)
  },
};

// ----------------------------------------------------------------------
// 🎲 تاس (Dice Roller)
// ----------------------------------------------------------------------

export const DICE_CONFIG = {
  // 📐 سایز تاس نسبت به تخته
  sizeRatio: 0.35, // تاس 28% ارتفاع تخته
  // بزرگتر کنی: 0.32 یا 0.35
  // کوچکتر کنی: 0.24 یا 0.22

  // 📐 نسبت عرض به ارتفاع زمین تاس (canvas)
  canvasWidthRatio: 1.15, // عرض canvas = 1.53 برابر ارتفاع
  // عریض‌تر کنی: 1.7 یا 1.8
  // باریک‌تر کنی: 1.4 یا 1.3

  // 📱 کاهش سایز در موبایل
  mobileScale: 0.88, // موبایل 88% سایز دسکتاپ
  // بزرگتر کنی: 0.95 یا 1.0
  // کوچکتر کنی: 0.80 یا 0.75

  // 📍 موقعیت تاس روی تخته (Position)
  position: {
    // موبایل خیلی کوچک
    smallMobile: {
      top: 130, // فاصله از بالا (px)
      left: -5, // فاصله از چپ (px)
    },
    // موبایل
    mobile: {
      top: 155, // فاصله از بالا (px)
      left: -5, // فاصله از چپ (px) - منفی = بیرون زدن از تخته
    },
    // دسکتاپ
    desktop: {
      top: 185, // فاصله از بالا (px)
      left: -5, // فاصله از چپ (px)
    },
  },
};

// ----------------------------------------------------------------------
// 📝 راهنمای تغییر ابعاد
// ----------------------------------------------------------------------

/**
 * 🎯 برای بزرگ/کوچک کردن کل تخته:
 * BOARD_CONFIG.scale.desktop/mobile/tablet رو تغییر بده
 * 
 * 🎲 برای بزرگ/کوچک کردن تاس:
 * DICE_CONFIG.sizeRatio رو تغییر بده
 * 
 * 📍 برای جابجایی تاس:
 * DICE_CONFIG.position.desktop/mobile رو تغییر بده
 * 
 * 🔴 برای بزرگ/کوچک کردن مهره‌ها:
 * BOARD_ELEMENTS_CONFIG.checkerSize رو تغییر بده
 * 
 * 📏 برای تغییر عرض جایگاه‌ها:
 * BOARD_ELEMENTS_CONFIG.pointWidth رو تغییر بده
 */
