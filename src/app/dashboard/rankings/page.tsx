import { RankingsView } from 'src/sections/rankings/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Player Rankings | NardAria',
};

export default function RankingsPage() {
  return <RankingsView />;
}
