import { WalletSettingsView } from 'src/sections/admin/wallet-settings';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Wallet Settings | Admin - Nard Arena',
};

export default function WalletSettingsPage() {
  return <WalletSettingsView />;
}
