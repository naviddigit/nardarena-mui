import ProfileView from 'src/sections/dashboard/profile-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Profile - Nard Arena',
};

export default function DashboardProfilePage() {
  return <ProfileView />;
}
