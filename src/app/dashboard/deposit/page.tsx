import { DepositView } from 'src/sections/dashboard/deposit';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Deposit | Nard Arena',
};

export default function DepositPage() {
  return <DepositView />;
}
