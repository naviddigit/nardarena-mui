# 🎮 راهنمای بازی با AI (فرانت‌اند)

## ✅ تغییرات انجام شده

### 1️⃣ URL با Game ID یونیک
حالا هر بازی یه URL یونیک داره:
```
http://localhost:8083/game/ai?game-id=uuid-game-id
```

### 2️⃣ AI خودکار حرکت می‌کنه
- ✅ دیگه نمی‌تونی برای AI تاس بریزی
- ✅ AI خودش تاس میریزه
- ✅ AI خودش فکر می‌کنه (2-5 ثانیه)
- ✅ AI خودش مهره‌ها رو حرکت میده
- ✅ بعد از حرکت AI، نوبت برمی‌گرده بهت

### 3️⃣ نمایش وضعیت AI
- 🤖 نشان‌دهنده "AI is thinking..." وقتی AI داره فکر می‌کنه
- 🎯 سطح سختی AI نمایش داده میشه: "AI - MEDIUM"
- ⏱️ تایمر برای AI هم کار می‌کنه

## 🚀 نحوه استفاده

### مرحله 1: شروع بازی جدید
```
http://localhost:8083/game/ai
```
1. صفحه باز میشه
2. رنگ خودت رو انتخاب کن (White یا Black)
3. بازی شروع میشه و URL آپدیت میشه:
   ```
   http://localhost:8083/game/ai?game-id=abc123...
   ```

### مرحله 2: بازی کن
1. ✅ **تو:** تاس بریز (Roll Dice)
2. ✅ **تو:** مهره‌هات رو حرکت بده
3. ✅ **تو:** Done بزن
4. 🤖 **AI:** خودش تاس میریزه (1-2 ثانیه صبر کن)
5. 🤖 **AI:** فکر می‌کنه (2-5 ثانیه بسته به سختی)
6. 🤖 **AI:** مهره‌هاش رو حرکت میده
7. 🔄 **نوبت برمی‌گرده بهت** - برو مرحله 1

### مرحله 3: بازگشت به بازی
اگه صفحه رو بستی، با همون URL برگرد:
```
http://localhost:8083/game/ai?game-id=abc123...
```
بازی از همونجا ادامه پیدا می‌کنه!

## 🎯 ویژگی‌های جدید

### ✅ دکمه Roll Dice برای AI غیرفعال
```tsx
canRoll={false} // AI cannot roll manually
```

### ✅ دکمه Done و Undo برای AI غیرفعال
```tsx
canDone={false} // AI handles its own turns
canUndo={false} // AI cannot undo
```

### ✅ نمایش سطح سختی
```tsx
country={`AI - ${aiDifficulty}`} // نمایش: "AI - MEDIUM"
```

### ✅ Indicator "AI is thinking..."
```tsx
{isAIThinking && (
  <Box>
    <Spinner />
    <Typography>AI is thinking...</Typography>
  </Box>
)}
```

## 🔧 تنظیمات Backend

### API Base URL
```typescript
// src/services/game-persistence-api.ts
const API_BASE_URL = 'http://localhost:3002/api';
```

### سطوح سختی AI
```typescript
const [aiDifficulty] = useState<'EASY' | 'MEDIUM' | 'HARD' | 'EXPERT'>('MEDIUM');
```

می‌تونی این رو تغییر بدی یا از یه selector استفاده کنی.

## 📊 جریان کامل

```
1. کاربر: باز کردن /game/ai
   ↓
2. سیستم: انتخاب رنگ (White/Black)
   ↓
3. Backend: ساخت بازی AI (POST /api/game/create)
   ↓
4. Frontend: آپدیت URL با game-id
   ↓
5. کاربر: تاس میریزه
   ↓
6. کاربر: مهره حرکت میده
   ↓
7. Frontend: ارسال حرکت به Backend (POST /api/game/:id/move)
   ↓
8. Backend: ذخیره حرکت + تشخیص نوبت AI
   ↓
9. Backend: AI تاس میریزه (خودکار، 1 ثانیه delay)
   ↓
10. Backend: AI فکر می‌کنه (2-5 ثانیه)
   ↓
11. Backend: AI حرکت می‌کنه
   ↓
12. Backend: نوبت رو برمی‌گردونه به User
   ↓
13. Frontend: Polling هر 1 ثانیه، تشخیص تغییر
   ↓
14. Frontend: نمایش حرکت AI
   ↓
15. تکرار از مرحله 5
```

## 🐛 رفع مشکلات

### AI بازی نمی‌کنه؟

**چک کردن Console:**
```
🎯 It's AI's turn, waiting for automatic move...
🤖 Triggering AI move for game: abc123...
✅ AI moved: [{from: 17, to: 21}, ...] Dice: [4, 5]
```

**راه حل:**
1. مطمئن شو Backend روی پورت 3002 در حاله
2. مطمئن شو gameId صحیح هست
3. رفرش کن صفحه رو
4. Console رو چک کن برای خطا

### URL بدون game-id؟

**راه حل:**
1. یه بازی جدید شروع کن
2. رنگ انتخاب کن
3. URL خودکار آپدیت میشه

### AI خیلی سریع بازی می‌کنه؟

**تایمینگ AI:**
```typescript
// در backend: src/modules/game/game.service.ts
await this.aiPlayerService.simulateThinkingTime(difficulty);
```

- EASY: 0.5-1.5 ثانیه
- MEDIUM: 1-3 ثانیه  
- HARD: 2-5 ثانیه
- EXPERT: 3-7 ثانیه

## 📁 فایل‌های تغییر یافته

### Frontend
```
✅ src/services/game-persistence-api.ts
   - تغییر API_BASE_URL به :3002
   - اضافه aiDifficulty به CreateGameDto
   - متد triggerAIMove()

✅ src/hooks/use-ai-game.ts (NEW)
   - Hook مدیریت بازی AI
   - Polling برای تشخیص حرکت AI
   - createAIGame(), triggerAIMove()

✅ src/app/game/ai/page.tsx
   - استفاده از useSearchParams برای game-id
   - غیرفعال کردن Roll Dice برای AI
   - نمایش "AI is thinking..."
   - آپدیت خودکار URL
```

### Backend
```
✅ src/modules/game/game.service.ts
   - متد makeAIMove()
   - خودکار trigger بعد از recordMove()

✅ src/modules/game/game.controller.ts
   - endpoint: POST /api/game/:id/ai-move

✅ src/modules/game/ai/ai-player.service.ts
   - evaluateSafety(), evaluateBlocking(), etc.
```

## 🎮 نکات مهم

1. **همیشه از URL با game-id استفاده کن** برای بازگشت به بازی
2. **صبر کن AI بازی کنه** - معمولاً 2-5 ثانیه طول می‌کشه
3. **نمی‌تونی برای AI Roll کنی** - دکمه غیرفعاله
4. **Polling هر 1 ثانیه** - صفحه خودش آپدیت میشه

## 🔜 بهبودهای آینده

- [ ] WebSocket بجای Polling (Real-time)
- [ ] انیمیشن حرکت مهره‌های AI
- [ ] انتخاب سطح سختی از UI
- [ ] نمایش تاس AI روی صفحه
- [ ] Replay حرکت‌های AI با سرعت کند

---

**✅ حالا می‌تونی با AI بازی کنی!**  
فقط صفحه رو باز کن، رنگ انتخاب کن، و شروع کن - AI خودش باقی کارو انجام میده! 🚀
